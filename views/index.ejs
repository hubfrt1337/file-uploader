<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/output.css">
</head>


<body class=" font-roboto"></body>
<% if(user) { %>
    <!-- hidden form to add folders-->
    <div id="modal" class="fixed inset-0 hidden items-center justify-center bg-black/50 z-2">
        <div id="modalContent" class="bg-base p-6  rounded-xl w-66  ">
            <form class="rounded-xl w-46" action="/create-folder" method="post">
                <input type="hidden" name="userId" value="<%=user.id%>">
                <input class="p-2 block rounded-xl border-2 border-gray-200 bg-gray-100" type="text" id="name"
                    name="name" placeholder="name" required>
                <button id="closeModalBtn"
                    class="mt-3 bg-accent py-2 px-5 hover:text-secondary transition duration-200 text-base rounded-xl cursor-pointer"
                    type="submit">Create</button>
            </form>
        </div>
    </div>

    <!--hidden SIDE PANEL -->
    <div id="panel-container" class="fixed shadow-[ -4px_0_10px_rgba(0,0,0,0.2) ] right-0 top-0 bottom-0 p-10 w-80 h-full bg-base z-4 translate-x-full transition duration-500 ">
        <div id="close-panel" class=" text-right text-red-600">
            <img src="/images/remove.png" class="w-8 h-8 cursor-pointer hover:scale-125 transition duration-200 float-right" alt="X">
        </div>
        <div id="name-panel" class="mt-25 text-4xl mb-5"></div>
        <div id="url-panel" class="text-2xl hover:text-secondary transition duration-200">
            <a id="a-panel" target="_blank" href="">Open file</a>
        </div>
        <div id="created-at" class="mt-5 opacity-35"></div>
        <div class="py-4 px-8 text-center border-0 mt-6 bg-accent rounded-2xl text-base w-48 cursor-pointer text-2xl 
        hover:text-secondary transition duration-200">
            <a id="a-download" href="">Download</a>
        </div>
    </div>

    <div class="p-10 min-h-screen">

        <!-- Navigation -->
        <nav class="flex justify-between   items-center relative">
            <div class="rounded-3xl cursor-pointer hover:bg-gray-100 transition">
                <img class="w-10 h-10" id="icon-search" src="/images/search.png">
                
            </div>
            <div class="text-sm p-2 hidden  border-2 bg-white top-0 w-5/6 sm:w-auto rounded-2xl outline-0 absolute z-2 " id="search-container">
                    <input class="outline-0 cursor-pointer block" type="text" id="search" name="search" placeholder="Search for a File">
                </div>
            <div class="absolute hidden overflow-y-scroll w-1/3 z-4 p-2 top-10 bg-white border-gray-200 border-2 h-32 " id="results-container">
                <ul class="overflow-hidden" id="ul-container">
                    <li class="p-2 hover:bg-gray-200">Nazwa 1</li>
                    <li>nazwa 2</li>
                    <li>djdqdjqwnq</li>
                </ul>
            </div>
            <div class="text-3xl relative z-1 font-roboto"><a href="/">File Storage</a></div>
            <div class="right-nav">
                <% if(user) { %>
                    <a href="/logout">
                        <img class="w-10 h-10 cursor-pointer" src="/images/logout.png" alt="">
                    </a>
                    <% } else { %>
                        <img class="w-10 h-10 cursor-pointer" src="/images/login.png" alt="">
                        <%} %>
            </div>
        </nav>


        <!-- Buttons Storage/Files section-->
        <section class="flex mt-12 mb-8 justify-center rounded-xl items-center bg-gray-100">
            <button id="storageBtn" class="p-5 cursor-pointer flex-1 rounded-xl bg-accent">Storage</button>
            <button id="filesBtn" class="p-5 cursor-pointer flex-1 rounded-xl ">Files</button>
        </section>


        <main>
            <!-- Storage/Files section:   Circular progress or Files-->
            <section id="storageSection" class=" p-10 flex bg-gray-100 justify-center rounded-2xl items-center flex-col">
                <div class="text-xl">
                    <%= user.email %>
                </div>
                <div class="circular-progress mt-5 flex justify-center rounded-full items-center w-45 h-45 "
                    data-percentage="<%=num*4%>">
                    <div class="rounded-full bg-white absolute w-35 h-35 inner-circle"></div>
                    <div class="py-5 flex flex-col percentage relative">
                        <div class="flex justify-center items-center">
                            <div class="text-6xl text-secondary progress-number">0
                            </div>
                            <span class="text-secondary  text-2xl align-top">%</span>
                        </div>
                        <div>
                            <div class="text-2xl text-center">used</div>
                        </div>
                    </div>
                </div>
                <div class="flex justify-around gap-20 items-center mt-5">
                    <div class="flex justify-center gap-3 items-center">
                        <div class="w-5 h-5 bg-accent rounded-xs"></div>
                        <div class="flex flex-col justify-center items-center gap-1">
                            <div class="text-sm opacity-75">Created</div>
                            <div class="text-lg text-center"><%=num%> <% if(num > 1) { %>
                                Files <% } else { %> File <% } %> </div>
                        </div>
                    </div>
                    <div class="flex justify-center gap-3 items-center">
                        <div class="w-5 h-5 bg-black opacity-15 rounded-xs"></div>
                        <div class="flex flex-col justify-center items-center gap-1">
                            <div class="opacity-75 text-sm">Free</div>
                            <div class="text-lg text-center">25 Files</div>
                        </div>
                    </div>
                </div>
            </section>

            <section id="filesSection" class="p-10 bg-gray-100 hidden  rounded-2xl  flex-col">
                
            <div class="grid mt-4 gap-6 grid-cols-[repeat(auto-fill,minmax(80px,1fr))] justify-start">
                
                <% if(folderFiles){ %>
                    <% folderFiles.forEach(folder=> { %>
                        <% folder.files.forEach(file => { %>
                            <div data-name="<%=file.name%>"
                                data-url="<%=file.url%>"
                                data-createdat="<%=file.created_at%>"
                                class="flex files flex-col p-2 transition hover:scale-120 duration-200 cursor-pointer gap-0.5 justify-center items-center">
                                <img src="/images/document.png" class="h-16 w-18 block" alt="folder">
                                <div class="text-4l text-center">
                                    <%= file.name %>
                                </div>
                            </div>
                            
                       <% }) %>
                        <% })} %>
                        </div>
            </section>

            <!-- Folders section -->
            <section
                class="grid p-10 bg-gray-100  rounded-2xl  mt-5 gap-5 grid-cols-[repeat(auto-fill,minmax(80px,1fr))] justify-center ">
                <% if(folders){ %>
                    <% folders.forEach(folder=> { %>
                        <a href="/folder/<%=folder.id%>">
                            <div
                                class="flex flex-col p-2 transition hover:scale-120 duration-200 cursor-pointer gap-0.5 justify-center items-center">
                                <img src="/images/open-folder.png" class="h-16 w-18 block" alt="folder">
                                <div class="text-4l text-center">
                                    <%= folder.name %>
                                </div>
                            </div>
                        </a>
                        <% }) %>
                            <% } %>
                                <div id="openModalBtn" class="flex flex-col p-2  cursor-pointer gap-0.5">
                                    <div
                                        class="h-16 w-16 block m-auto  bg-accent text-6xl rounded-2xl text-base text-center transition duration-200 hover:text-secondary">
                                        +</div>
                                    <div class="text-lg text-center">Add</div>
                                </div>
            </section>
        </main>
    </div>



    <!---->
    <!-- if user doesn't exists || Another page
        -->

    <%} else { %>
        <div class="min-h-screen bg-[url('/images/background-5.jpg')] bg-cover bg-center
            flex justify-center items-center p-10 ">
            <div class="bg-base p-10 py-15 shadow-2xl  rounded-4xl flex flex-col justify-center items-center">
                <h1 class="text-secondary font-bold mb-5 text-center  text-3xl">Simplify your file storage</h1>
                <div class="text-sm text-center opacity-55">Upload, manage and organize your files in one place</div>
                <button
                    class="bg-accent rounded-3xl mt-5 cursor-pointer w-full py-4 px-10 text-3xl font-bold text-shadow  text-base"><a
                        href="/log-in">Let's go</a>
                </button>
            </div>
        </div>
        <% } %>

                        <script src="/scripts/progress-circle.js"></script>
                        <script src="/scripts/showModal.js"></script>
                        <script src="/scripts/storageFilesButton.js"></script>
                        <script src="/scripts/panel.js"></script>
                        <script src="/scripts/search.js"></script>
                        </body>

</html>